# The sample requests a clock producer through the clock control API. The interesting part
# is per case: see the overlays (and conf files, where extra Kconfig is needed) under
# boards/ and the scenarios in tests.yaml. Each case selects a consumer via the clk-consumer
# alias and points its clocks property at a SoC clock state.
CONFIG_CLOCK_CONTROL=y
