/*
 * Case: nRF54H20, pdm130 (pdm0) as the consumer, base clock on the 16 MHz peripheral clock (PCLK)
 * from fll16m in its open-loop (low-accuracy) default.
 *
 * pdm0 is only used to carry the clock state; the sample does not operate the PDM. The
 * pclk16m_hfint state maps to the PCLK mux position and, having no producer, takes no clock action
 * (fll16m open-loop is the automatic default).
 */
/ {
	aliases {
		clk-consumer = &pdm0;
	};
};

&pinctrl {
	pdm0_default_alt: pdm0_default_alt {
		group1 {
			psels = <NRF_PSEL(PDM_CLK, 1, 7)>,
				<NRF_PSEL(PDM_DIN, 1, 8)>;
		};
	};
};

&pdm0 {
	status = "okay";
	pinctrl-0 = <&pdm0_default_alt>;
	pinctrl-names = "default";
	memory-regions = <&cpuapp_dma_region>;
	clocks = <&pclk16m_hfint>;
};
