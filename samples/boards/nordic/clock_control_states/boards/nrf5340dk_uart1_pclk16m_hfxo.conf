# Use the split clock control driver (nordic,nrf-clock-hfclk) rather than the legacy
# CLOCK_CONTROL_NRF, since the pclk16m_hfxo state's producer is the &hfclk device that the
# sample requests through the clock control API.
CONFIG_CLOCK_CONTROL_NRF=n
