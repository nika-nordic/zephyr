# Compile the TDM driver so the clock-state prototype in i2s_nrf_tdm.c is instantiated for the
# selected tdm node. The sample still only reads the clock state(s) and requests the producer(s)
# through the clock control API; the driver derives the same mux/producer/frequency at build time.
CONFIG_I2S=y
