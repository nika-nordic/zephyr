# Use the split clock control driver (nordic,nrf-clock-hfclkaudio) rather than the legacy
# CLOCK_CONTROL_NRF, since the aclk_hfclkaudio state's producer is the &hfclkaudio device that the
# sample requests through the clock control API.
CONFIG_CLOCK_CONTROL_NRF=n
