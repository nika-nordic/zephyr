# Compile the PDM (DMIC) driver so the clock-state prototype in dmic_nrfx_pdm.c is instantiated for
# the selected pdm node. The sample still only reads the clock state and requests the producer (if
# any) through the clock control API; the driver derives the same mux/producer/frequency at build
# time.
CONFIG_AUDIO_DMIC=y
