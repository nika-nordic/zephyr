# The interesting configuration is per board, see the boards/ overlays and conf files.
# Each board selects a UARTE via the clk-showcase-uart alias, points it at a clock
# producer with the nordic,clock-producer property, and enables a
# CONFIG_UART_NRFX_UARTE_CLOCK_MGMT_* scheme so the producer is requested.
